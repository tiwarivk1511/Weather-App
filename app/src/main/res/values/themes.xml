<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Weather" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Weather" parent="Base.Theme.Weather" />

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
    <!-- Customize text appearance -->
    <item name="android:textColor">@color/black</item>
    <item name="android:textColorHint">@color/black</item>

        <!-- Customize background -->
    <item name="android:background">@drawable/frame_bg</item>

    <!-- Customize search icon -->
    <item name="searchIcon">@drawable/search</item>
    <item name="searchHintIcon">@drawable/search</item>
    <item name="android:fontFamily">serif</item>
    <!-- Customize close icon -->
    <item name="closeIcon">@drawable/baseline_clear_24</item>

</style>


</resources>